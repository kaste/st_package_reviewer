- Binding is missing the keys {'command'}
    File: Default.sublime-keymap
    Binding: {"keys": ["ctrl+x"]}
- Binding is missing the keys {'keys'}
    File: Default.sublime-keymap
    Binding: {"command": "lolk"}
- 'keys' key is empty or not a list
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": "alts+x"}
- Invalid modifier key 'alts'
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": ["alts+x"]}
- Invalid key 'nope'
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": ["ctrl+k", "nope"]}
- The binding ['ctrl+b'] has no context. Packages should only ship key bindings that use a specific context such as 'selector', 'setting.*', or a custom context key. If the binding defines a main entry-point to your package, move it to an example keymap instead so users can decide on their own.
    File: Default.sublime-keymap
- Multiple bindings, e.g. ['super+alt+v'] and ['super+alt+up'], have no context. Packages should only ship key bindings that use a specific context such as 'selector', 'setting.*', or a custom context key. If these bindings define main entry-points to your package, move them to an example keymap instead so users can decide on their own.
    File: Default (OSX).sublime-keymap
- '<character>' bindings must have a 'context'
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": ["<character>"]}
- Invalid key 'Ü'
    File: Default.sublime-keymap
    Binding: {"command": "hurf_durf", "keys": ["ctrl+\u00dc"]}
- Single-chord modifier bindings are disallowed
    File: Default.sublime-keymap
    Binding: {"command": "hurf_durf", "keys": ["ctrl"]}
