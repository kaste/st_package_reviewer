- Binding is missing the keys {'command'}
    File: Default.sublime-keymap
    Binding: {"keys": ["ctrl+x"]}
- Binding is missing the keys {'keys'}
    File: Default.sublime-keymap
    Binding: {"command": "lolk"}
- 'keys' key is empty or not a list
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": "alts+x"}
- Invalid modifier key 'alts'
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": ["alts+x"]}
- Invalid key 'nope'
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": ["ctrl+k", "nope"]}
- The binding ['ctrl+b'] is global or only uses broad context keys. Loaded key bindings must use a specific context such as 'selector', 'setting.*', or a custom context key. Otherwise move it to an example keymap.
    File: Default.sublime-keymap
- The binding ['super+alt+v'] is global or only uses broad context keys. Loaded key bindings must use a specific context such as 'selector', 'setting.*', or a custom context key. Otherwise move it to an example keymap.
    File: Default (OSX).sublime-keymap
- The binding ['super+alt+up'] is global or only uses broad context keys. Loaded key bindings must use a specific context such as 'selector', 'setting.*', or a custom context key. Otherwise move it to an example keymap.
    File: Default (OSX).sublime-keymap
- The binding ['super+alt+s'] is global or only uses broad context keys. Loaded key bindings must use a specific context such as 'selector', 'setting.*', or a custom context key. Otherwise move it to an example keymap.
    File: Default (OSX).sublime-keymap
- '<character>' bindings must have a 'context'
    File: Default.sublime-keymap
    Binding: {"command": "noop", "keys": ["<character>"]}
- Invalid key 'Ü'
    File: Default.sublime-keymap
    Binding: {"command": "hurf_durf", "keys": ["ctrl+\u00dc"]}
- Single-chord modifier bindings are disallowed
    File: Default.sublime-keymap
    Binding: {"command": "hurf_durf", "keys": ["ctrl"]}
